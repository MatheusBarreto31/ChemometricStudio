{
  "display_name": "CV Configuration",
  "category": "3.Configuration",
  "data_type": "configuration",
  "input_aliases": {},
  "output_aliases": {
    "cv_config": "Cross-Validation Configuration"
  },
  "short_description": "Configure cross-validation strategy for downstream modeling functions",
  "long_description": "Set up cross-validation parameters once and route to any compatible modeling function. Disable with use_cv=False to run without CV.\n\nData Compatibility:\nWorks with univariate (2D) and multiway (3D, 4D, etc.) data:\n- Univariate: (n_samples, n_variables) or (n_samples, n_responses)\n- Multiway: (n_samples, n_wavelengths, n_time) or (n_samples, n_wavelengths, n_time, n_electrodes), etc.\nSplitting always occurs on axis 0 (samples), preserving all other dimensions.\n\nSupported Strategies:\n1. K-Fold: Divides data into k equal folds; trains on k-1, validates on 1\n2. Stratified K-Fold: Preserves class proportions in each fold (for classification)\n3. Time Series: Forward-chaining validation respecting temporal order (no lookahead bias)\n4. Repeated K-Fold: Multiple iterations of K-Fold with different random splits\n5. Shuffle Split: Random repeated train/test splits (good for large datasets)\n6. Venetian Windows: Creates k overlapping windows that cover the full dataset\n7. Moving Window: Progressive sliding windows for time-dependent data\n8. LOOCV (Leave-One-Out): Each sample left out once; most thorough but expensive\n9. Bootstrap (OOB): Random sampling with replacement; Out-of-Bag test set\n\nNote: Reference keys, metrics, and captured outputs are configured by each model function, not here.",
  "setup": {
    "categories": ["Activation", "Strategy", "Parameters"],
    "layout": [
      {
        "name": "use_cv",
        "label": "Enable Cross-Validation",
        "category": "Activation",
        "widget": "checkbutton",
        "type": "bool",
        "default": true,
        "required": true,
        "input_type": "user",
        "tooltip": "Enable/disable CV; if false, downstream functions run single fit on all data"
      },
      {
        "name": "cv_strategy",
        "label": "CV Strategy",
        "category": "Strategy",
        "widget": "combobox",
        "type": "str",
        "default": "kfold",
        "values": ["kfold", "stratified_kfold", "timeseries", "repeated_kfold", "shuffle_split", "venetian_windows", "moving_window", "loocv", "bootstrap"],
        "value_aliases": ["K-Fold (Standard)", "Stratified K-Fold (Classification)", "Time Series (Forward Chaining)", "Repeated K-Fold", "Shuffle Split (Random)", "Venetian Windows", "Moving Window", "LOOCV (Leave-One-Out)", "Bootstrap (OOB)"],
        "required": true,
        "input_type": "user",
        "visible_if": {"use_cv": true},
        "tooltip": "Which CV splitting strategy to use"
      },
      {
        "name": "n_splits",
        "label": "Number of Splits/Folds",
        "category": "Parameters",
        "widget": "entry",
        "type": "int",
        "default": "5",
        "required": true,
        "input_type": "user",
        "visible_if": {"use_cv": true, "cv_strategy": ["kfold", "stratified_kfold", "timeseries", "repeated_kfold", "shuffle_split", "venetian_windows", "moving_window", "bootstrap"]},
        "tooltip": "Typical: 3-10 (more folds = more compute time). For LOOCV, this is auto-set to n_samples. For Bootstrap, number of iterations."
      },
      {
        "name": "random_state",
        "label": "Random State (Seed)",
        "category": "Parameters",
        "widget": "entry",
        "type": "int",
        "default": "42",
        "required": false,
        "input_type": "user",
        "visible_if": {"use_cv": true},
        "tooltip": "For reproducibility; leave blank for random seed"
      },
      {
        "name": "window_size",
        "label": "Window Size (Moving/Venetian Windows)",
        "category": "Parameters",
        "widget": "entry",
        "type": "int",
        "default": "",
        "required": false,
        "input_type": "user",
        "visible_if": {"use_cv": true, "cv_strategy": ["moving_window", "venetian_windows"]},
        "tooltip": "Window size for moving window or venetian windows. If blank, auto-calculated as n_samples / n_splits"
      },
      {
        "name": "n_repeats",
        "label": "Number of Repeats (Repeated K-Fold)",
        "category": "Parameters",
        "widget": "entry",
        "type": "int",
        "default": "10",
        "required": false,
        "input_type": "user",
        "visible_if": {"use_cv": true, "cv_strategy": ["repeated_kfold"]},
        "tooltip": "Number of times to repeat K-Fold with different random splits (default: 10)"
      },
      {
        "name": "test_size",
        "label": "Test Set Size (Shuffle Split)",
        "category": "Parameters",
        "widget": "entry",
        "type": "float",
        "default": "0.2",
        "required": false,
        "input_type": "user",
        "visible_if": {"use_cv": true, "cv_strategy": ["shuffle_split"]},
        "tooltip": "Proportion of samples for test set (default: 0.2 = 20%; range: 0.0-1.0)"
      },
      {
        "name": "shuffle",
        "label": "Shuffle Before Splitting",
        "category": "Parameters",
        "widget": "checkbutton",
        "type": "bool",
        "default": true,
        "required": false,
        "input_type": "user",
        "visible_if": {"use_cv": true, "cv_strategy": ["kfold", "stratified_kfold", "repeated_kfold", "venetian_windows", "moving_window"]},
        "tooltip": "Randomize order before splitting (recommended for IID data; not applicable to Time Series, LOOCV, or Bootstrap)"
      }
    ]
  }
}
